%% Отключение лимита на ожидание consumer ack (длинные прогоны бенчмарка).
%% В rabbitmq.conf доступны только целые миллисекунды; «выкл» — только так.
%% https://www.rabbitmq.com/docs/consumers#acknowledgement-timeout
[
  {rabbit, [
    {consumer_timeout, undefined}
  ]}
].
